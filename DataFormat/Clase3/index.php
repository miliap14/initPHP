<?php

    //CLASE SOBRE EXTRACCIÓN DE DATOS

    //$data = "Estudio PHP";
    //echo $data[0]; //Se imprime solo la letra E

    //$post = "Lorem ipsum dolor sit amet consectetur adipisicing elit. Quaerat nisi nulla aliquam vitae in dolore maxime eligendi ab, debitis sapiente quis dolorum nostrum neque at natus esse quisquam omnis ipsa!";
    //$extract = substr($post, 0, 20); //obtiene solo una parte del texto, desde el caracter 0 al 20

    //echo "$extract... [ver más]";

    // $data = 'javascript, php, laravel';
    // $tags = explode(', ', $data); //nos permite crear un array a partir de un string indicando cual es el separador
    // echo "<pre>"; //Etiqueta para formato exlusiva de PHP
    // var_dump($tags);

    // $cursos = ['Javascript','PHP','Laravel'];
    // echo implode(', ', $cursos); //Convierte un array en un string. Le indicamos como se separan y el array que queremos afectar.

    // $cursos = "     Curso de PHP    ";
    // echo trim($cursos); ///Elimina los espacios en blanco innecesarios de las variables.
    // echo ltrim($cursos); ///Elimina los espacios en blanco del lado izquierdo
    // echo rtrim($cursos); ///Elimina los espacios en blanco del lado derecho

?>